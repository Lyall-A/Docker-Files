services:
  jellyfin-vue:
    image: ghcr.io/jellyfin/jellyfin-vue:unstable
    container_name: jellyfin-vue
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8099:80
    env_file:
      - ../../Volumes/jellyfin-vue/.env
    environment:
      - DISABLE_SERVER_SELECTION=1
